services:
  - type: web
    name: quote-portal
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: QUOTE_DATA_DIR
        value: /tmp/quote-data
      - key: PYTHONUNBUFFERED
        value: "1"
